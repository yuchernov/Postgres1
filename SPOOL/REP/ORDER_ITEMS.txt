CREATE TABLE IF NOT EXISTS OT.ORDER_ITEMS (
ORDER_ID NUMERIC (12,0),
ITEM_ID NUMERIC (12,0),
PRODUCT_ID NUMERIC (12,0),
QUANTITY NUMERIC (8,2),
UNIT_PRICE NUMERIC (8,2)
) tablespace SOGECAP; alter table OT.ORDER_ITEMS owner to OT;
alter table OT.ORDER_ITEMS alter column PRODUCT_ID set NOT NULL;
alter table OT.ORDER_ITEMS alter column QUANTITY set NOT NULL;
alter table OT.ORDER_ITEMS alter column UNIT_PRICE set NOT NULL;
alter table OT.ORDER_ITEMS add constraint PK_ORDER_ITEMS primary key (ORDER_ID, ITEM_ID);
CREATE UNIQUE INDEX IF NOT EXISTS PK_ORDER_ITEMS on OT.ORDER_ITEMS (ITEM_ID, ORDER_ID) TABLESPACE USERS;
alter index OT.PK_ORDER_ITEMS owner to OT;
